<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.breadev.baseproj.android.presentation">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:name=".base.view.application.BaseApplication"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme" >

        <!-- SECURITY -->
        <activity
            android:name=".module.security.view.activity.SplashActivity"
            android:theme="@style/StyleSplash">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".module.security.view.activity.AccessActivity" />
    </application>
</manifest>
